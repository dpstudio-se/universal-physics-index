# Theory and fundamentals (exploratory)

**Status: `SYM` / `HYP` sketches only — not EST physics.**

Files in this folder are placeholders or exploratory notes related to optional
blueprint themes (G2 language, information motifs, etc.). They are **not**
repository authority, not experimental results, and not a Theory of Everything.

Prefer:

1. `src/upi/` + `schemas/` + `tests/` for engineering truth  
2. top-level `README.md` (identity-protected)  
3. `data/examples/*.json` with explicit `status` labels  

If a note lacks equations, evidence, and falsification criteria, treat it as
`STOP` or `SYM`, never as established science.
